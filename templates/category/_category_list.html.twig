<div class="flex gap-1 flex-wrap">
    {% for category in categories %}
        <div class="flex gap-1 items-center bg-blue-100 text-blue-800 text-sm font-medium px-2.5 py-0.5 rounded">
            {{ category.name }}
        </div>
    {% endfor %}
</div>